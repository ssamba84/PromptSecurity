# --- Prompt Security text inspection API ---
PROMPT_SECURITY_URL=https://eu.prompt.security/api/protect
# Your Prompt Security APP-ID (credential). Copy this file to .env and paste it in.
# .env is gitignored, so the key is never committed. Provided with the assignment,
# or generated in the Prompt Security admin portal.
PROMPT_SECURITY_APP_ID=
